function caulcular(){
    let verificar = Number.parseInt(window.prompt('Digite o número aqui: '))

    console.log(verificar)

    // let resultado = document.getElementById('resultado')

    // resultado.innerHTML = `<p>O número ${verificar} é ${resultado}</p>`

    // if(verificar % 2 ){
    //     resultado.innerHTML = `par` 
    // } else {
    //     resultado.innerHTML = `impar`
    // }

}